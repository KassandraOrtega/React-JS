// fn delcaration
function add(x, y){
    return x + y;
}

// fn expression
var add = function(x,y){
    return x + y;
}

// Is the same as the expression above
 var add = (x,y) => {
    return x + y;
 }

 add(1,2)